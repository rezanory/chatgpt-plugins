[project]
name = "chatgpt-plugins-core"
version = "0.1.0"
description = "Provider-neutral contracts for ChatGPT plugins"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chatgpt_plugins_core"]
