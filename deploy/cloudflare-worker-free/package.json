{
  "name": "chatgpt-kaggle-cloudflare-worker-free",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "npm run types && tsc --project tsconfig.json --noEmit",
    "deploy:dry": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.0",
    "wrangler": "4.123.0"
  }
}
