name: cloudflare-worker-free-deploy-disabled-v622-finalization

# Disabled during PNEUMONIA V6.2.2 finalization.
# This workflow intentionally performs no Cloudflare mutation and uses no GitHub-hosted compute.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm disabled state
        shell: powershell
        run: |
          Write-Host 'Generic Cloudflare deployment is disabled during V6.2.2 finalization.'
          Write-Host 'No Cloudflare mutation is performed by this workflow.'
          Write-Host 'No GitHub-hosted compute is used.'
