name: v622-self-hosted-governance-source-bundle

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v622-self-hosted-governance-source-bundle.yml'
      - 'scripts/v622_collect_governance_sources.py'
      - 'deploy/cloudflare-worker-free/src/v622-governance-source-worker.ts'

permissions:
  contents: read
  issues: write

concurrency:
  group: v622-cloudflare-production-mutation
  cancel-in-progress: false

jobs:
  collect:
    runs-on: [self-hosted, windows, x64, v622]
    environment: cloudflare-production
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Resolve local Python
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $candidates = @()
          $command = Get-Command python.exe -ErrorAction SilentlyContinue
          if ($command) { $candidates += $command.Source }
          if (-not [string]::IsNullOrWhiteSpace($env:RUNNER_TOOL_CACHE)) {
            $pythonRoot = Join-Path $env:RUNNER_TOOL_CACHE 'Python'
            if (Test-Path $pythonRoot) {
              $candidates += Get-ChildItem -Path $pythonRoot -Filter python.exe -Recurse -ErrorAction SilentlyContinue |
                Sort-Object FullName -Descending |
                Select-Object -ExpandProperty FullName
            }
          }
          $python = $candidates | Where-Object { $_ -and (Test-Path $_) } | Select-Object -First 1
          if (-not $python) { throw 'No runnable Python executable found on self-hosted runner.' }
          & $python --version
          [System.IO.File]::AppendAllText($env:GITHUB_ENV, "V622_PYTHON=$python`n", [System.Text.UTF8Encoding]::new($false))

      - name: Deploy protected governance source bridge
        shell: powershell
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          RAW_CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          $ErrorActionPreference = 'Stop'
          $accountId = ([string]$env:RAW_CLOUDFLARE_ACCOUNT_ID).Trim()
          if ([string]::IsNullOrWhiteSpace($env:CLOUDFLARE_API_TOKEN) -or [string]::IsNullOrWhiteSpace($accountId)) {
            throw 'Cloudflare deployment credentials are not configured.'
          }
          Write-Host "::add-mask::$accountId"
          $env:CLOUDFLARE_ACCOUNT_ID = $accountId
          [System.IO.File]::AppendAllText($env:GITHUB_ENV, "V622_CF_ACCOUNT_ID=$accountId`n", [System.Text.UTF8Encoding]::new($false))
          Push-Location 'deploy/cloudflare-worker-free'
          try {
            npm install --no-audit --no-fund
            if ($LASTEXITCODE -ne 0) { throw 'npm install failed' }
            npm run check
            if ($LASTEXITCODE -ne 0) { throw 'Worker typecheck failed' }
            npx wrangler deploy src/v622-governance-source-worker.ts --name chatgpt-kaggle-gateway
            if ($LASTEXITCODE -ne 0) { throw 'Governance source bridge deploy failed' }
          }
          finally { Pop-Location }

      - name: Create ephemeral governance capability
        shell: powershell
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'
          $token = (& $env:V622_PYTHON -c "import secrets; print(secrets.token_urlsafe(48))").Trim()
          if ([string]::IsNullOrWhiteSpace($token)) { throw 'Could not generate governance capability.' }
          Write-Host "::add-mask::$token"
          $env:CLOUDFLARE_ACCOUNT_ID = $env:V622_CF_ACCOUNT_ID
          Push-Location 'deploy/cloudflare-worker-free'
          try {
            $token | npx wrangler secret put CGP_PROJECT_CONTROL_TOKEN
            if ($LASTEXITCODE -ne 0) { throw 'Could not install governance capability.' }
          }
          finally { Pop-Location }
          [System.IO.File]::AppendAllText($env:GITHUB_ENV, "V622_GOVERNANCE_TOKEN=$token`n", [System.Text.UTF8Encoding]::new($false))

      - name: Recover canonical governance source bundle
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          & $env:V622_PYTHON scripts/v622_collect_governance_sources.py
          if ($LASTEXITCODE -ne 0) { throw 'Governance source recovery failed.' }

      - name: Upload private governance source bundle
        uses: actions/upload-artifact@v4
        with:
          name: v622-governance-source-bundle
          path: ${{ runner.temp }}/v622-governance-source-bundle
          if-no-files-found: error
          retention-days: 3
          compression-level: 6

      - name: Publish governance bundle receipt
        shell: powershell
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $body = "## V6.2.2 governance source bundle ready`n`n- Run: $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID`n- Artifact: v622-governance-source-bundle`n- Canonical W01 governance source package only.`n- No Kaggle compute, locked test, or external cohort access.`n"
          $payload = @{ body = $body } | ConvertTo-Json
          $headers = @{ Authorization = "Bearer $env:GITHUB_TOKEN"; Accept = 'application/vnd.github+json'; 'X-GitHub-Api-Version' = '2022-11-28' }
          Invoke-RestMethod -Uri "https://api.github.com/repos/$env:GITHUB_REPOSITORY/issues/18/comments" -Method Post -Headers $headers -ContentType 'application/json' -Body $payload | Out-Null

      - name: Remove capability and restore canonical Worker
        if: always()
        shell: powershell
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          $ErrorActionPreference = 'Continue'
          if (-not [string]::IsNullOrWhiteSpace($env:V622_CF_ACCOUNT_ID)) {
            $env:CLOUDFLARE_ACCOUNT_ID = $env:V622_CF_ACCOUNT_ID
            Push-Location 'deploy/cloudflare-worker-free'
            try {
              '{"CGP_PROJECT_CONTROL_TOKEN":null}' | Set-Content -Encoding utf8 "$env:RUNNER_TEMP\v622-delete-control.json"
              npx wrangler secret bulk "$env:RUNNER_TEMP\v622-delete-control.json"
              Remove-Item "$env:RUNNER_TEMP\v622-delete-control.json" -Force -ErrorAction SilentlyContinue
              npx wrangler deploy
              if ($LASTEXITCODE -ne 0) { Write-Error 'Canonical Worker restore deploy failed.' }
            }
            finally { Pop-Location }
          }

      - name: Remove local governance bundle
        if: always()
        shell: powershell
        run: |
          Remove-Item "$env:RUNNER_TEMP\v622-governance-source-bundle" -Recurse -Force -ErrorAction SilentlyContinue
