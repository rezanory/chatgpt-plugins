name: v622-external-kaggle-status-probe-retired-after-incident

# Root cause captured: NIH sample dataset is mounted without the assumed sample.zip.
# This diagnostic workflow is retired before the governed repair run.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm incident probe retired
        shell: powershell
        run: |
          Write-Host 'External Kaggle incident root cause is already captured in Issue 18.'
          Write-Host 'No Cloudflare/Kaggle/model mutation is performed.'
