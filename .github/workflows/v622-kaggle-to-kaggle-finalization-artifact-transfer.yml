name: v622-kaggle-to-kaggle-finalization-artifact-transfer-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired transfer path
        shell: bash
        run: |
          echo 'RETIRED: artifact Dataset already exists inside Kaggle.'
          echo 'Dataset: trickermark/pneumonia-v6-2-2-finalization-artifacts'
          echo 'azadka READER access has been verified. Do not transport checkpoints through GitHub/Cloudflare.'
