name: v622-kaggle-to-kaggle-finalization-artifact-transfer-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Retired transfer path
        shell: powershell
        run: |
          Write-Host 'RETIRED: artifact Dataset already exists inside Kaggle.'
          Write-Host 'Dataset: trickermark/pneumonia-v6-2-2-finalization-artifacts'
          Write-Host 'azadka READER access has been verified. Do not transport checkpoints through GitHub/Cloudflare.'
