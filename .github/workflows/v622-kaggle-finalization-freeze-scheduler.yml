name: v622-kaggle-finalization-freeze-scheduler-paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm pause
        run: |
          echo 'Paused while selected artifacts are moved into a private Kaggle dataset owned by azadka.'
          echo 'No finalization, locked-test, external, training, HPO, or confirmation compute is launched.'
