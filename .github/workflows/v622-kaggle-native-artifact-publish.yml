name: v622-kaggle-native-artifact-publish-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired publisher path
        shell: bash
        run: |
          echo 'RETIRED: Kaggle-native artifact publication already created the finalization Dataset.'
          echo 'Dataset: trickermark/pneumonia-v6-2-2-finalization-artifacts'
          echo 'Proceed only with azadka finalization master.'
