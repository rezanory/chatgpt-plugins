name: v622-finalization-reconcile-v1-retired

# Superseded by canonical reconciliation v2. Manual no-op only before lockbox.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired V6.2.2 reconciliation v1: no Cloudflare mutation, Kaggle compute, locked test, or external validation is performed.'
