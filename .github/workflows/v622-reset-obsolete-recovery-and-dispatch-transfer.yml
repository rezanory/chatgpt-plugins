name: v622-reset-obsolete-recovery-and-dispatch-transfer-retired

# Retired after Kaggle-native artifact custody and final freeze completed.
# Manual no-op only; cannot cancel the shared lane, deploy Cloudflare, or restart transfer.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired V6.2.2 recovery reset: no concurrency cancellation, Cloudflare mutation, Kaggle transfer, locked test, or external validation is performed.'
