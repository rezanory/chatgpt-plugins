name: v622-reconcile-v2-dispatch-retired

# Reconciliation passed. Manual no-op only; no further reconciliation dispatch is allowed before lockbox.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired V6.2.2 reconciliation dispatcher: no Actions dispatch, Cloudflare mutation, Kaggle compute, locked test, or external validation is performed.'
