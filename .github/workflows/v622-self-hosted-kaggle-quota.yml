# Retired: provider runtime credentials are never passed through GitHub Actions.
name: v622-self-hosted-kaggle-quota-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired provider quota workflow.'
          Write-Host 'No credentials are read and no provider compute is launched.'
          Write-Host 'Quota and account health are checked through the read-only Cloudflare gateway.'
