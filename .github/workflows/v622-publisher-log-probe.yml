name: v622-publisher-log-probe-retired

# Artifact publishing is complete. This old log probe deployed the shared Cloudflare Worker and is no longer permitted.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired V6.2.2 publisher log bridge: no Cloudflare mutation, Kaggle compute, locked test, or external validation is performed.'
