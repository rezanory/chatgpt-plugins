name: v622-m07-exact-seven-direct-retired

# Retired: Kaggle runtime credentials belong only in Cloudflare Worker Secrets.
# This historical launch path must not accept issue comments or launch compute.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, windows, x64, v622]
    steps:
      - name: Confirm retired state
        shell: powershell
        run: |
          Write-Host 'Retired M07 seven-account launch path.'
          Write-Host 'No provider credentials are read and no Kaggle compute is launched.'
          Write-Host 'Use the read-only Cloudflare recovery monitor and the approved finalization gates.'
