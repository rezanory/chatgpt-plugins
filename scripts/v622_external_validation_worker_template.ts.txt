import canonicalWorker from './index';
import { kernelStatus, listKernels, type WorkerEnv } from './kaggle';
import { EXTERNAL_VALIDATION_SCRIPT, EXTERNAL_VALIDATION_SCRIPT_SHA256, W16_NOTEBOOK_SHA256, W16_SOURCE_ZIP_SHA256 } from './v622-external-validation-payload';

type Env = WorkerEnv & { CGP_PROJECT_CONTROL_TOKEN?: string };
type Rec = Record<string, unknown>;
const API_ROOT='https://api.kaggle.com/v1/kernels.KernelsApiService';
const OWNER='azadka';
const TARGET_SLUG='pneumonia-v6-2-2-external-validation';
const TARGET_REF=`${OWNER}/${TARGET_SLUG}`;
const TARGET_TITLE='PNEUMONIA V6.2.2 EXTERNAL VALIDATION';
const ARTIFACT_DATASET='trickermark/pneumonia-v6-2-2-finalization-artifacts';
const DEV_DATASET='paultimothymooney/chest-xray-pneumonia';
const CHEXPERT_DATASET='ashery/chexpert';
const NIH_DATASET='nih-chest-xrays/sample';
const FINALIZATION_REF='azadka/pneumonia-v6-2-2-finalization-master';
const RECONCILIATION_REF='azadka/pneumonia-v6-2-2-final-freeze-reconciliation';
const LOCKED_TEST_REF='azadka/pneumonia-v6-2-2-official-locked-test';
const FINAL_MANIFEST_SHA='2313023819c9eec669d835b99dcc2524b03bb0da236c2ec8f98c4e1221948414';
const RECONCILIATION_SHA='99a319c8d397342aaefd3c15e0eb8f968a973738f55ef97925a2a16da8c485d8';
const LOCKED_COMPLETION_SHA='2ecc67f4268f812604e51db5b46d2ece36f9de12ff7baf3cf0bbbc8df4a58ada';
const ACTIVE=new Set(['RUNNING','QUEUED','STARTING','PENDING']);
const TERMINAL_BAD=new Set(['ERROR','FAILED','CANCELLED','CANCELED','DEAD']);
function rec(v:unknown):Rec{return v&&typeof v==='object'&&!Array.isArray(v)?v as Rec:{}}
function json(v:unknown,status=200):Response{return new Response(JSON.stringify(v),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}
function authorized(r:Request,e:Env):boolean{const t=e.CGP_PROJECT_CONTROL_TOKEN?.trim();return Boolean(t&&t.length>=32&&r.headers.get('authorization')===`Bearer ${t}`)}
function authHeader(t:string):string{return t.startsWith('KGAT_')?`Bearer ${t}`:`Basic ${btoa(`${OWNER}:${t}`)}`}
function norm(raw:Rec):string{const s=rec(raw.session);for(const v of [raw.status,raw.statusName,raw.state,raw.sessionStatus,s.status,s.statusName,s.state])if(typeof v==='string'&&v.trim())return v.trim().toUpperCase();return'UNKNOWN'}
function allowedOutputUrl(raw:string):URL{const u=new URL(raw),h=u.hostname.toLowerCase();if(u.protocol!=='https:'||!(h==='api.kaggle.com'||h==='www.kaggle.com'||h==='storage.googleapis.com'||h.endsWith('.kaggleusercontent.com')||h.endsWith('.googleusercontent.com')))throw new Error('output URL host not allowlisted');return u}
async function sha256Text(text:string):Promise<string>{const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text));return Array.from(new Uint8Array(d),b=>b.toString(16).padStart(2,'0')).join('')}
async function saveKernel(env:Env):Promise<Rec>{
 const token=env.CGP_KAGGLE_MASTER_TOKEN?.trim();if(!token)throw new Error('master Kaggle token missing');
 const body:Rec={slug:TARGET_REF,newTitle:TARGET_TITLE,text:EXTERNAL_VALIDATION_SCRIPT,language:'python',kernelType:'script',kernelExecutionType:'SAVE_AND_RUN_ALL',isPrivate:true,enableGpu:true,enableTpu:false,enableInternet:false,kernelDataSources:[FINALIZATION_REF,RECONCILIATION_REF,LOCKED_TEST_REF],datasetDataSources:[ARTIFACT_DATASET,DEV_DATASET,CHEXPERT_DATASET,NIH_DATASET],competitionDataSources:[],modelDataSources:[]};
 const response=await fetch(`${API_ROOT}/SaveKernel`,{method:'POST',headers:{Authorization:authHeader(token),'Content-Type':'application/json','User-Agent':'chatgpt-v622-external-validation/1.0'},body:JSON.stringify(body)});const text=await response.text();let value:Rec={};try{value=rec(text?JSON.parse(text):{})}catch{throw new Error(`SaveKernel non-JSON HTTP ${response.status}`)}const code=typeof value.code==='number'?value.code:undefined;if(!response.ok||(code!==undefined&&code>=400))throw new Error(String(value.message??`SaveKernel HTTP ${response.status}`).slice(0,1600));const badD=Array.isArray(value.invalidDatasetSources)?value.invalidDatasetSources:[];const badK=Array.isArray(value.invalidKernelSources)?value.invalidKernelSources:[];if(badD.length||badK.length)throw new Error(`SaveKernel rejected sources: datasets=${JSON.stringify(badD)} kernels=${JSON.stringify(badK)}`);return value;
}
async function outputs(env:Env):Promise<Rec[]>{const token=env.CGP_KAGGLE_MASTER_TOKEN?.trim();if(!token)throw new Error('master Kaggle token missing');const response=await fetch(`${API_ROOT}/ListKernelSessionOutput`,{method:'POST',headers:{Authorization:authHeader(token),'Content-Type':'application/json','User-Agent':'chatgpt-v622-external-validation/1.0'},body:JSON.stringify({userName:OWNER,kernelSlug:TARGET_SLUG,pageSize:100})});const payload=rec(await response.json());if(!response.ok)throw new Error(`ListKernelSessionOutput HTTP ${response.status}`);return Array.isArray(payload.files)?payload.files.map(rec):[]}
async function receipt(env:Env):Promise<Rec|null>{
 const files=await outputs(env);const required=['EXTERNAL_VALIDATION_SUMMARY.json','EXTERNAL_VALIDATION_SUMMARY.sha256','EXTERNAL_INDEPENDENCE_AUDIT.json','chexpert_valid__REPORT.json','nih_pneumonia_vs_no_finding__REPORT.json'];for(const name of required)if(!files.some(x=>String(x.fileName??'').endsWith(name)))return null;
 const sr=files.find(x=>String(x.fileName??'').endsWith('EXTERNAL_VALIDATION_SUMMARY.json')&&typeof x.url==='string'),hr=files.find(x=>String(x.fileName??'').endsWith('EXTERNAL_VALIDATION_SUMMARY.sha256')&&typeof x.url==='string');if(!sr||!hr)return null;
 const textResp=await fetch(allowedOutputUrl(String(sr.url)),{redirect:'follow'});if(!textResp.ok)throw new Error(`summary download HTTP ${textResp.status}`);const text=await textResp.text();const hashResp=await fetch(allowedOutputUrl(String(hr.url)),{redirect:'follow'});if(!hashResp.ok)throw new Error(`summary sha download HTTP ${hashResp.status}`);const declared=(await hashResp.text()).trim().toLowerCase();const actual=await sha256Text(text);if(actual!==declared)throw new Error('external validation summary SHA mismatch');const v=rec(JSON.parse(text));const cohorts=rec(v.cohorts);
 if(v.project!=='PNEUMONIA V6.2.2'||v.stage!=='EXTERNAL_VALIDATION'||v.status!=='PASS'||v.final_freeze_manifest_sha256!==FINAL_MANIFEST_SHA||v.final_freeze_reconciliation_sha256!==RECONCILIATION_SHA||v.official_locked_test_completion_sha256!==LOCKED_COMPLETION_SHA)throw new Error('external validation immutable binding mismatch');
 if(v.selection_locked!==true||v.threshold_tuning_performed!==false||v.calibration_fitting_performed!==false||v.weight_fitting_performed!==false||v.training_performed!==false||v.reselection_performed!==false||v.official_locked_test_feedback_used!==false||v.external_data_used!==true||v.next_permitted_stage!=='FINAL_CLOSURE')throw new Error('external validation governance mismatch');
 if(!cohorts.chexpert_valid||!cohorts.nih_pneumonia_vs_no_finding)throw new Error('expected external cohorts missing');
 return{status:'PASS',stage:'EXTERNAL_VALIDATION',summary_sha256:actual,cohorts:v.cohorts,independence_audit:v.independence_audit,next_permitted_stage:v.next_permitted_stage};
}
async function launch(env:Env):Promise<Rec>{const existing=await listKernels(env,'master',TARGET_SLUG,100);const exact=existing.some(x=>String(rec(x).ref??'').toLowerCase()===TARGET_REF.toLowerCase());if(exact){const status=norm(await kernelStatus(env,'master',TARGET_REF));if(ACTIVE.has(status))return{action:'existing_active',target_kernel_ref:TARGET_REF,status,submitted:false};if(status==='COMPLETE'){const rc=await receipt(env);if(rc)return{action:'existing_complete',target_kernel_ref:TARGET_REF,status,submitted:false,receipt:rc};throw new Error('external validation COMPLETE without validated receipt; automatic rerun forbidden')}if(TERMINAL_BAD.has(status))throw new Error(`external validation already terminal ${status}; automatic rerun forbidden`);throw new Error(`external validation exists with ambiguous status ${status}; automatic rerun forbidden`)}const result=await saveKernel(env);return{action:'created_and_submitted_once',target_kernel_ref:TARGET_REF,result,submitted:true,kaggle_compute_kind:'EXTERNAL_VALIDATION',gpu:true,internet:false,cohorts:[CHEXPERT_DATASET,NIH_DATASET],training:false,hpo:false,confirmation:false,reselection:false,threshold_tuning:false,script_sha256:EXTERNAL_VALIDATION_SCRIPT_SHA256,w16_notebook_sha256:W16_NOTEBOOK_SHA256,w16_source_zip_sha256:W16_SOURCE_ZIP_SHA256}}
export default{async fetch(request:Request,env:Env,ctx:ExecutionContext):Promise<Response>{const url=new URL(request.url);if(url.pathname==='/healthz'&&request.method==='GET')return json({service:'v622-external-validation',status:'ready',protected:true,target:TARGET_REF,cohorts:[CHEXPERT_DATASET,NIH_DATASET]});if(url.pathname.startsWith('/control/v6-2-2/external-validation/')&&!authorized(request,env))return new Response('Forbidden',{status:403});try{if(url.pathname==='/control/v6-2-2/external-validation/launch'&&request.method==='POST')return json({project:'PNEUMONIA V6.2.2',...(await launch(env))});if(url.pathname==='/control/v6-2-2/external-validation/status'&&request.method==='POST'){const status=norm(await kernelStatus(env,'master',TARGET_REF));const rc=status==='COMPLETE'?await receipt(env):null;return json({project:'PNEUMONIA V6.2.2',target_kernel_ref:TARGET_REF,status,receipt:rc,automatic_rerun_forbidden:true})}return canonicalWorker.fetch(request,env,ctx)}catch(error){return json({project:'PNEUMONIA V6.2.2',ok:false,error:error instanceof Error?error.message.slice(0,1800):'unknown error',target_kernel_ref:TARGET_REF,automatic_rerun_forbidden:true,training:false,hpo:false,confirmation:false,reselection:false},502)}}} satisfies ExportedHandler<Env>;
